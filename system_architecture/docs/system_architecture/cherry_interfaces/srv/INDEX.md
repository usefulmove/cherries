# docs/system_architecture/cherry_interfaces/srv/

Directory containing 4 analyzed file(s).

## Files

- [Detectionhdr.md](Detectionhdr.md): A ROS 2 service interface definition (.srv) for an HDR cherry detection service, accepting six HDR-bracketed stereo camera images with encoder metadata and returning detected cherry data with keypoint overlays. — `ros2`, `service-interface`, `hdr-imaging`
- [INDEX.md](INDEX.md): Index of ROS 2 service definition files for the cherry detection and harvesting system, documenting request/response contracts for image processing, encoder data, hardware control, and detection services. — `ros2-services`, `cherry-detection`, `robotics-interfaces`
- [ImageSave.md](ImageSave.md): A ROS 2 service definition file that specifies a service for saving images, accepting a sensor image as input and returning a result code indicating the outcome. — `ros2`, `service`, `image-processing`
- [Trigger.md](Trigger.md): A ROS 2 service definition file (.srv) for a simple Trigger service that accepts an integer ID as a request and returns an integer status as a response. — `ros2`, `service`, `robotics`
