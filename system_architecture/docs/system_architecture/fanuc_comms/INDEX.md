# docs/system_architecture/fanuc_comms/

Directory containing 2 analyzed file(s).

## Files

- [package.md](package.md): A ROS 2 package manifest (package.xml) for the fanuc_comms package, defining its metadata, maintainer, and test dependencies using the ament build system. — `ros2`, `ament`, `package-manifest`
- [setup.md](setup.md): Python setuptools configuration for the fanuc_comms ROS 2 (ament) package, which provides communication utilities for Fanuc robots and registers a console script entry point. — `ros2`, `ament`, `robotics`
