experiment:
  name: efficientnet_b2_label_smooth_seed42
  description: "EfficientNet-B2 with label smoothing (alpha=0.1) - Seed 42"
  id: EXP-003B
  seed: 42

data:
  root: "../cherry_classification/data"
  batch_size: 32
  num_workers: 4
  input_size: 128
  normalize: false
  augmentation: true

model:
  architecture: efficientnet_b2
  pretrained: true
  num_classes: 2
  freeze_backbone: false

training:
  epochs: 30
  optimizer: adam
  learning_rate: 0.0001
  weight_decay: 0.00001
  use_scheduler: false
  device: cuda
  mixed_precision: false
  label_smoothing: 0.1

checkpointing:
  save_every: 5
  save_best: true
  output_dir: "./experiments/efficientnet_b2_label_smooth_seed42"

logging:
  log_file: "metrics.json"
  print_every: 1
  save_confusion_matrix: true
